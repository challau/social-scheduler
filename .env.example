# Base URL of the backend API.
# Local dev: http://localhost:8000 (also the fallback if unset)
# Production (set in Vercel dashboard): https://your-backend.onrender.com
VITE_API_URL=http://localhost:8000
